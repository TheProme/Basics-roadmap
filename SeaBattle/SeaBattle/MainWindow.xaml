<Window x:Class="SeaBattle.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SeaBattle"
        xmlns:c="clr-namespace:SeaBattle.Controls"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0">
            <c:Ship ShipModel="{Binding ShipViewModel1}" Background="Green" VerticalAlignment="Center" HorizontalAlignment="Center"/>
            <c:Ship ShipModel="{Binding ShipViewModel2}" Background="Green" VerticalAlignment="Center" HorizontalAlignment="Center"/>
            <c:Ship ShipModel="{Binding ShipViewModel3}" Background="Green" VerticalAlignment="Center" HorizontalAlignment="Center"/>
            <c:Ship ShipModel="{Binding ShipViewModel4}" Background="Green" VerticalAlignment="Center" HorizontalAlignment="Center"/>
        </StackPanel>
        <Grid Grid.Row="1">
            <c:Field FieldModel="{Binding FieldView}" />
        </Grid>
    </Grid>
    
</Window>
