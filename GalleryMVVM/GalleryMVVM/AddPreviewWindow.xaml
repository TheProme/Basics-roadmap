<Window x:Class="GalleryMVVM.AddPreviewWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:GalleryMVVM"
        mc:Ignorable="d"
        Title="AddPreviewWindow">
    <StackPanel>
        <ListBox ItemsSource="{Binding PreviewImages}" ItemsPanel="{StaticResource GalleryItemsPanel}">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <local:ImageView/>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <Button Content="Apply" x:Name="applyButton" Click="applyButton_Click" Grid.Column="0"/>
            <Button Content="Cancel" x:Name="cancelButton" Click="cancelButton_Click" Grid.Column="1"/>
        </Grid>
    </StackPanel>
</Window>
